package cn.edu.bnuz.seawas.dao;

import cn.edu.bnuz.seawas.entity.Classification;

import java.util.List;

/**
 * 分类表DAO接口
 * Created by sjk on 2017-07-28.
 */
public interface ClassificationMapper {

    /**
     * 删除数据
     * @param classification classification
     */
    int delete(Classification classification);

    /**
     * 插入数据
     * @param classification classification
     */
    int insert(Classification classification);

    /**
     * 非空插入
     * @param classification classification
     */
    int insertSelective(Classification classification);

    /**
     * 获取单条数据
     * @param classification classification
     */
    Classification get(Classification classification);

    /**
     * 非空更新
     * @param classification classification
     */
    int updateByPrimaryKeySelective(Classification classification);

    /**
     * 更新
     * @param classification classification
     */
    int update(Classification classification);

    /**
     * 列表查询
     * @param classification classification
     */
    List<Classification> findList(Classification classification);
}